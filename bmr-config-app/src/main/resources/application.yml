server:
  port: 4643
  address: 0.0.0.0
spring:
  profiles:
    active: dev
  application:
    name: bmr-config-server
  servlet:
    multipart:
      max-request-size: 20GB
      max-file-size: 20GB

logging:
  level:
    root: info